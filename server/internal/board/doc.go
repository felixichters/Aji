// Package board owns the fixed-grid representation and low-level
// placement primitives: cells, stones, neighbor iteration, emptiness
// checks. It knows nothing about players, turns, or networking.
package board
